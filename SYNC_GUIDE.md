# 文章同步指南

本指南教你如何从现有的笔记仓库同步文章到博客。

## 🎯 方案概览

提供三种同步方式：

1. **自动化脚本**（推荐）- 一键同步，自动更新配置
2. **手动复制** - 简单直接，适合少量文章
3. **Git Submodule** - 高级方案，保持笔记仓库独立

---

## 方案一：自动化脚本同步（推荐）⭐

### 1. 配置笔记仓库路径

编辑 `sync-articles.js` 文件，修改配置：

```javascript
const CONFIG = {
  // 修改为你的笔记仓库路径
  notesRepoPath: 'C:/Users/11790/your-notes-repo',
  
  // 要同步的文件夹
  syncFolders: [
    'frontend',      // 前端笔记
    'backend',       // 后端笔记
    'algorithms',    // 算法笔记
  ],
  
  // 或者指定具体文件
  specificFiles: [
    'frontend/vue3-notes.md',
    'algorithms/binary-search.md',
  ],
}
```

### 2. 运行同步脚本

**方式 A - 使用 npm 命令**：
```bash
npm run sync
```

**方式 B - 双击运行**：
```
双击 sync-articles-simple.bat
```

**方式 C - 直接运行**：
```bash
node sync-articles.js
```

### 3. 功能特性

- ✅ 自动复制 Markdown 文件
- ✅ 自动处理图片路径
- ✅ 自动复制图片资源
- ✅ 自动更新侧边栏配置
- ✅ 支持文章分类

### 4. 配置说明

| 配置项 | 说明 | 示例 |
|--------|------|------|
| `notesRepoPath` | 笔记仓库的路径 | `C:/Users/xxx/notes` |
| `syncFolders` | 要同步的文件夹列表 | `['frontend', 'backend']` |
| `specificFiles` | 要同步的具体文件 | `['vue3.md']` |
| `copyAssets` | 是否复制图片 | `true` |
| `autoUpdateSidebar` | 是否自动更新侧边栏 | `true` |

---

## 方案二：手动复制文章

### 1. 找到要发布的文章

在你的笔记仓库中选择要发布的 Markdown 文件。

### 2. 复制到博客

将文件复制到博客的 `blog/` 目录：

```
your-notes-repo/
  └── frontend/
      └── vue3-notes.md

      ↓ 复制

blog/
  └── blog/
      └── vue3-notes.md
```

### 3. 处理图片（如果有）

如果文章包含图片：

1. 创建 `public/assets/` 目录（如果不存在）
2. 复制图片到 `public/assets/`
3. 修改 Markdown 中的图片路径：

```markdown
# 修改前
![示例](../assets/example.png)

# 修改后
![示例](/assets/example.png)
```

### 4. 更新侧边栏

编辑 `.vitepress/config.mts`，在 `sidebar` 中添加文章：

```typescript
sidebar: {
  '/blog/': [
    {
      text: '前端开发',
      items: [
        { text: 'Vue 3 笔记', link: '/blog/vue3-notes' },
        // 添加新文章
      ]
    }
  ]
}
```

---

## 方案三：Git Submodule（高级）

### 适用场景

- 笔记仓库也是 Git 管理
- 希望保持笔记仓库独立
- 需要在多个地方使用笔记

### 1. 添加 Submodule

```bash
cd blog
git submodule add <你的笔记仓库URL> notes
```

### 2. 创建软链接

**Windows (管理员权限运行)**：
```powershell
New-Item -ItemType SymbolicLink -Path "blog/frontend" -Target "notes/frontend"
```

**Mac/Linux**：
```bash
ln -s ../notes/frontend blog/frontend
```

### 3. 更新笔记

```bash
git submodule update --remote
```

---

## 📂 推荐的笔记仓库结构

```
your-notes-repo/
├── frontend/           # 前端笔记
│   ├── vue3.md
│   ├── react.md
│   └── typescript.md
├── backend/            # 后端笔记
│   ├── nodejs.md
│   └── python.md
├── algorithms/         # 算法笔记
│   └── sorting.md
├── assets/             # 图片资源
│   ├── vue3/
│   └── react/
└── README.md
```

---

## 🎨 文章格式建议

### 标准的文章格式

```markdown
# 文章标题

> 发布日期: 2025-01-15

## 简介

这是文章简介...

## 主要内容

### 小标题 1

内容...

### 小标题 2

内容...

## 总结

总结内容...
```

### 图片引用

```markdown
# 使用相对路径（同步脚本会自动转换）
![示例](../assets/example.png)

# 或使用绝对路径
![示例](/assets/example.png)
```

---

## 🔄 工作流程示例

### 日常写作流程

1. **在笔记仓库写作**
   ```bash
   cd your-notes-repo
   # 编辑文章
   ```

2. **准备发布时运行同步**
   ```bash
   cd blog
   npm run sync
   ```

3. **预览效果**
   ```bash
   npm run dev
   ```

4. **推送到 GitHub**
   ```bash
   git add .
   git commit -m "添加新文章"
   git push
   ```

---

## 🛠️ 常见问题

### Q1: 图片显示不出来？

**原因**: 图片路径不正确

**解决**:
1. 确保图片在 `public/assets/` 目录
2. 使用 `/assets/xxx.png` 路径
3. 运行 `npm run sync` 自动处理

### Q2: 侧边栏没有更新？

**原因**: 没有运行同步脚本或手动更新配置

**解决**:
1. 运行 `npm run sync`（自动更新）
2. 或手动编辑 `.vitepress/config.mts`

### Q3: 同步后样式乱了？

**原因**: Markdown 格式不标准

**解决**:
1. 确保标题层级正确（# ## ###）
2. 代码块使用三个反引号
3. 检查是否有特殊字符

### Q4: 想排除某些笔记不同步？

**解决**:
1. 使用 `specificFiles` 指定要同步的文件
2. 或在 `syncFolders` 中只包含需要的文件夹

---

## 📝 配置模板

### 场景 1: 同步整个文件夹

```javascript
const CONFIG = {
  notesRepoPath: 'C:/Users/xxx/notes',
  syncFolders: ['frontend', 'backend'],
  specificFiles: [],
}
```

### 场景 2: 只同步精选文章

```javascript
const CONFIG = {
  notesRepoPath: 'C:/Users/xxx/notes',
  syncFolders: [],
  specificFiles: [
    'frontend/vue3-best-practices.md',
    'backend/nodejs-performance.md',
    'algorithms/leetcode-top100.md',
  ],
}
```

### 场景 3: 混合模式

```javascript
const CONFIG = {
  notesRepoPath: 'C:/Users/xxx/notes',
  syncFolders: ['frontend'],  // 同步整个前端文件夹
  specificFiles: [
    'backend/important-note.md',  // 加上后端的一篇精选
  ],
}
```

---

## 🚀 快速开始

1. **修改配置**
   ```bash
   # 编辑 sync-articles.js，设置 notesRepoPath
   ```

2. **第一次同步**
   ```bash
   npm run sync
   ```

3. **查看效果**
   ```bash
   npm run dev
   ```

4. **以后每次写完文章**
   ```bash
   npm run sync  # 一键同步
   ```

---

## 💡 最佳实践

1. **保持笔记仓库独立** - 笔记和博客分离，笔记可以更自由
2. **定期同步** - 写完文章后立即同步，避免遗忘
3. **使用分类** - 在 `categoryMapping` 中维护好分类
4. **备份重要文章** - 笔记仓库也要做好版本控制
5. **图片优化** - 发布前压缩图片，提升加载速度

---

## 🔗 相关文档

- [VitePress 官方文档](https://vitepress.dev/)
- [Markdown 语法指南](https://markdown.com.cn/)
- [Git Submodule 教程](https://git-scm.com/book/zh/v2/Git-%E5%B7%A5%E5%85%B7-%E5%AD%90%E6%A8%A1%E5%9D%97)

---

**祝你写作愉快！** ✍️
