# VitePress 博客搭建指南

## 简介

VitePress 是一个基于 Vite 和 Vue 3 的静态站点生成器，专为技术文档和博客设计。它具有出色的性能和开发体验。

## 为什么选择 VitePress？

### 优势

- ⚡ **极速开发** - 基于 Vite，开发服务器启动快，热更新迅速
- 📝 **Markdown 优先** - 专注内容创作，支持 Markdown 扩展
- 🎨 **主题美观** - 默认主题现代化，支持深色模式
- 🔍 **内置搜索** - 本地全文搜索，无需配置
- 📱 **响应式设计** - 完美适配各种设备

## 快速开始

### 1. 安装依赖

```bash
npm install -D vitepress vue
```

### 2. 初始化配置

创建 `.vitepress/config.mts` 配置文件：

```typescript
import { defineConfig } from 'vitepress'

export default defineConfig({
  title: "我的博客",
  description: "技术博客",
  themeConfig: {
    nav: [
      { text: '首页', link: '/' },
      { text: '博客', link: '/blog/' }
    ]
  }
})
```

### 3. 创建内容

创建 `index.md` 作为首页：

```markdown
---
layout: home
hero:
  name: "我的博客"
  tagline: 记录技术成长
---
```

### 4. 启动开发服务器

```bash
npm run dev
```

## 进阶配置

### 自定义主题

VitePress 支持完全自定义主题，你可以：

- 修改默认主题样式
- 创建自定义布局
- 添加 Vue 组件

### SEO 优化

```typescript
export default defineConfig({
  head: [
    ['meta', { name: 'keywords', content: '技术博客,前端开发' }],
    ['meta', { property: 'og:title', content: '我的博客' }]
  ]
})
```

### 部署到 GitHub Pages

使用 GitHub Actions 自动部署，详见项目根目录的部署配置。

## 总结

VitePress 是搭建技术博客的优秀选择，它让你专注于内容创作，同时提供现代化的阅读体验。

## 参考资源

- [VitePress 官方文档](https://vitepress.dev/)
- [Vue 3 文档](https://cn.vuejs.org/)
- [Vite 文档](https://cn.vitejs.dev/)
