# 我的技术博客 (My Tech Blog)

基于 VitePress 搭建的现代化个人技术博客，集成了自动化发布工具，支持一键分发到多个技术平台。

## ✨ 特性

- ⚡ **极速体验** - 基于 Vite + Vue 3，开发和构建都超快
- 📝 **结构化知识库** - 清晰的目录分类（基础、语言、架构、工程化等）
- 🚀 **自动化发布** - 内置 Puppeteer 脚本，支持一键发布文章到掘金、CSDN 等平台
- 🎨 **美观主题** - 现代化设计，支持深色模式与响应式布局
- 🔍 **全文搜索** - 内置本地搜索功能
- � **RSS 订阅** - 自动生成 RSS Feed，方便读者订阅

## 📁 项目结构

```text
blog/
├── .vitepress/          # VitePress 配置与主题
│   ├── config.mts       # 核心配置（导航、侧边栏）
│   ├── theme/           # 自定义主题样式
│   └── genFeed.ts       # RSS 生成脚本
├── blog/                # 博客内容目录（按领域分类）
│   ├── fundamentals/    # 计算机基础 (算法, 网络, DB)
│   ├── languages/       # 编程语言 (JS/TS, Python, Go, Rust)
│   ├── system-design/   # 系统设计 (模式, 分布式)
│   ├── engineering/     # 工程化 (DevOps, 工具)
│   ├── ai/              # AI 工程 (LLM, Agents)
│   ├── resources/       # 学习资源 (书籍, 课程)
│   └── workflows/       # 工作流与效能
├── scripts/             # 自动化脚本目录
│   ├── publisher/       # 多平台发布器 (Puppeteer)
│   │   ├── juejin-publisher.js
│   │   └── ...
│   ├── publish-to-platforms.js  # 发布入口脚本
│   └── ...
├── public/              # 静态资源
├── index.md             # 网站首页
└── package.json         # 项目依赖与命令
```

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 本地开发

```bash
npm run dev
```

访问 `http://localhost:5173/blog/` 查看效果。

### 构建生产版本

```bash
npm run build
```

## 🤖 自动化多平台发布

本项目包含一套基于 Puppeteer 的自动化发布工具，可以帮你将文章自动同步到掘金、CSDN 等平台。

### 使用方法

运行以下命令启动交互式发布流程：

```bash
npm run pub:multi
```

脚本会引导你：

1. 输入要发布的 Markdown 文件路径
2. 选择要发布的目标平台
3. 自动打开浏览器并模拟用户操作进行发布

> **注意**：首次使用时，脚本会启动一个 Chrome 浏览器，请手动登录各平台账号。登录状态（Cookie）会被保存在本地 (`~/.trae-blog-publisher/chrome-data`)，后续无需重复登录。

## 📝 写作指南

### 1. 选择分类

请根据文章内容，将 `.md` 文件放入 `blog/` 下对应的子目录中。例如：

- React/Vue 相关 -> `blog/languages/js-ts/`
- Docker/K8s 相关 -> `blog/engineering/devops/`

### 2. 编写内容

支持标准 Markdown 语法与 Vue 组件。

### 3. 配置导航

如果需要，修改 `.vitepress/config.mts` 中的 `sidebar` 配置，将新文章加入侧边栏。

## 📄 License

MIT License
